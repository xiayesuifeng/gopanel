image: archlinux/archlinux:base-devel

variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_DEPTH: 1

before_script:
  - pacman -Sy --noconfirm git go wget unzip

stages:
  - build

build-gopanel:
  stage: build
  script:
    - wget https://gitlab.com/xiayesuifeng/gopanel-web/builds/artifacts/master/download?job=build-web -O web.zip
    - unzip web.zip
    - mv build $CI_PROJECT_DIR/web
    - mkdir -p $CI_PROJECT_DIR/gopanel
    - go build --tags web -o $CI_PROJECT_DIR/gopanel/gopanel
    - cp -rf contrib/systemd $CI_PROJECT_DIR/gopanel

  artifacts:
    paths:
      - $CI_PROJECT_DIR/gopanel